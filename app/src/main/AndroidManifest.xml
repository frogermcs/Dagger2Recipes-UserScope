<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.frogermcs.recipes.dagger_userscope"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!--Required by Github API client -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".user.details.UserDetailsActivity"/>
        <activity android:name=".user.repositories.RepositoriesListActivity"/>
    </application>

</manifest>